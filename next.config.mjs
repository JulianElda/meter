/** @type {import('next').NextConfig} */
const nextConfig = {
  /*
  basePath: "/meter",

  async redirects() {
    return [
      {
        source: "/",
        destination: "/meter",
        basePath: false,
        permanent: false,
      },
    ];
  },
  */
};

export default nextConfig;
